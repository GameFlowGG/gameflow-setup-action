name: Test

on:
  push:
  pull_request:

jobs:
  test:
    name: Install and verify
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run gameflow-setup-action (no auth)
        uses: ./

      - name: Confirm binary is on PATH
        run: gameflow --version
